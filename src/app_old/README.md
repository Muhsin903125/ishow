# Legacy Route Archive

`src/app_old` is preserved only as historical reference while the live app runs from `src/pages`.

Rules:

- Do not add new feature work here.
- Do not point new tasks or docs here as an active runtime surface.
- Treat any remaining imports from this tree as legacy cleanup candidates.
